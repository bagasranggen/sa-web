import React from 'react';

import NotFoundIndex from '@/components/pages/NotFoundIndex';

export type NotFoundProps = {};

const NotFound = ({}: NotFoundProps): React.ReactElement => {
    return <NotFoundIndex />;
};

export default NotFound;
